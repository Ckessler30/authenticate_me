




const initalState = {}

const questionReducer = (state=initalState, action) => {
    let newState;
    switch(action.type){
        default:
            return state
    }
}


export default questionReducer